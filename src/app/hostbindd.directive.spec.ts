import { HostbinddDirective } from './hostbindd.directive';

describe('HostbinddDirective', () => {
  it('should create an instance', () => {
    const directive = new HostbinddDirective();
    expect(directive).toBeTruthy();
  });
});
