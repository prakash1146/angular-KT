import { UserendererDirective } from './userenderer.directive';

describe('UserendererDirective', () => {
  it('should create an instance', () => {
    const directive = new UserendererDirective();
    expect(directive).toBeTruthy();
  });
});
